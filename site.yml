---
# MAIN: ###############################
- name: Fedora Workstation Configuration
  hosts: localhost
  # Allow Root Access:
  become: yes

# ROLES: ##############################
  roles:
    - System
    - User
    - App-Packages
    - App-Config
    - Desktop-Customisation